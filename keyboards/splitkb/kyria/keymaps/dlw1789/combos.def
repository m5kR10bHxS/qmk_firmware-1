// Germ's gboards combo dictionary:
// http://combos.gboards.ca/docs/install/
// https://docs.qmk.fm/features/combo

// Global Combos: Referenced to layer 0 key positions
COMB(ER_ESC,       KC_ESC,      KC_E,     KC_R)
//COMB(DF_XXX,       KC_XXX,      KC_D,     KC_F)
COMB(CV_BSPC,      KC_BSPC,     KC_T,     KC_Y)
COMB(UI_TAB,       KC_TAB,      KC_U,     KC_I)
//COMB(JK_XXX,       KC_XXX,      KC_J,     KC_K)
COMB(MCOM_DEL,     KC_DEL,      KC_B,     KC_N)
COMB(TY_CTRL,      C(KC_Q),     KC_C,     KC_V)
COMB(GH_ENT,       KC_ENT,      KC_G,     KC_H)
COMB(BN_CTRL,      C(KC_Z),     KC_M,     KC_COMM)
COMB(AG_LEAD,      QK_LEAD,     KC_BKSP,  TG(_CFG)  // split ergo
//COMB(AG_LEAD,      QK_LEAD,     TG(_NAV), TG(_NUM)  // ANSI slab
//COMB(VM_CAPSWD,    CW_TOGG,     KC_V,     KC_M)     // splt ergo
//COMB(VN_CAPSWD,    CW_TOGG,     KC_V,     KC_N)     // ANSI slab


// LSHFT + RSHFT = caps word  =  Default Caps Word activation.
// Necessary to configure a combo for Caps Word, so as to work with both Workman and Titanium - Currently handled in the Caps Word config.
// The index fingers always toggle CW on, regardless of the key map.
// VM is convenient for split ergo boards and VN is convenient for slab boards.
// Note: Additional configuration is needed if using Mods as part of the combo trigger.
